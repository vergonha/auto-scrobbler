{
  "name": "scrobblas",
  "version": "1.0.0",
  "description": "",
  "main": "build/scrobble.js",
  "scripts": {
    "build": "esbuild `find backend \\( -name '*.js' \\)` --bundle --outdir=build --platform=node",
    "start": "node build/scrobble.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "dotenv": "^16.0.3",
    "esbuild": "0.17.10",
    "md5": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
